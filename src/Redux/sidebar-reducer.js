let initialState = {
    user:[
        {id:1, name:"Kate"},
        {id:2, name:"Valera"},
        {id:3, name:"Mark"}
    ]
};


const sidebarReducer = (state = initialState, action)=> {
    return state;
};



export default sidebarReducer;